import { FormaConCurva } from './js/object3D/FormaConCurva.js';

export class Grua{
    constructor(gl, glProgram, viewMatrix, projMatrix, dibGeo){
        this.gl = gl;
        this.glProgram = glProgram;
        this.viewMatrix = viewMatrix;
        this.projMatrix = projMatrix;
        this.dibGeo = dibGeo; 
    }

    draw(){
        
    }

} 